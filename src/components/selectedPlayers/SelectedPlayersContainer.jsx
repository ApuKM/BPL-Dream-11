import React from 'react';

const SelectedPlayersContainer = ({selectedPlayers, setSelectedPlayers}) => {
    console.log(selectedPlayers)
    return (
        <div>
            <h2>Selected Playeres</h2>
        </div>
    );
};

export default SelectedPlayersContainer;